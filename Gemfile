source "https://rubygems.org"

# GitHub Pages compatible build (pins Jekyll + plugin versions to what Pages runs)
gem "github-pages", group: :jekyll_plugins

# Needed to `jekyll serve` locally on Ruby 3.0+
gem "webrick", "~> 1.8"
